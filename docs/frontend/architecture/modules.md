[🏠 Home](../../../index.md) > [Frontend](../../index.md) > [Architecture](./index.md)

# 🏗️ Frontend Modules

Описание логики JavaScript модулей.

## `js/upload.js`
Отвечает за страницу загрузки (`upload_form.html`).

**Функционал:**
1.  **Drag & Drop:** Обрабатывает события `dragover`, `dragleave`, `drop` на зоне загрузки.
2.  **File Validation:** Проверяет MIME-тип файла (разрешены `image/jpeg`, `image/png`, `image/gif`).
3.  **Preview:** Читает файл через `FileReader` и конвертирует в Base64 (для локального прототипа).
4.  **Save:** Вызывает `saveImageToStorage` из `storage.js`.
5.  **UI Updates:** Показывает кнопку "Images", если в галерее появились файлы.

## `js/images.js`
Отвечает за страницу галереи (`images.html`).

**Функционал:**
1.  **Render:** Получает список файлов через `getGalleryData()`.
2.  **DOM Manipulation:** Генерирует HTML-разметку для каждой картинки (иконка, имя, ссылка, кнопки).
3.  **Actions:**
    *   **Copy Link:** Копирует фейковую ссылку в буфер обмена.
    *   **Delete:** Вызывает `removeImageFromStorage` и перерисовывает список.

## `js/storage.js`
Слой абстракции данных (Data Access Layer).
В текущей версии работает как **Mock API** поверх `localStorage`.

**Методы:**
*   `getGalleryData()`: Возвращает массив объектов файлов.
*   `saveImageToStorage(fileObj)`: Добавляет новый файл в массив и сохраняет в localStorage.
*   `removeImageFromStorage(index)`: Удаляет файл по индексу.

---
[⬅️ Назад к Frontend](../index.md)
