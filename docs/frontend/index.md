[🏠 Home](../index.md)

# 🎨 Frontend Documentation

Клиентская часть приложения **PinLite**.
Реализована на **Vanilla JS** (ES6+) без использования фреймворков (React/Vue) для максимальной простоты и легковесности.

## 🛠️ Технологический стек
*   **HTML5:** Семантическая верстка, использование `<dialog>` для модальных окон.
*   **CSS3:** Flexbox, Grid, CSS Variables, `scrollbar-gutter`.
*   **JavaScript:** ES6 Modules, Async/Await, Fetch API.
*   **API:** REST API (FastAPI Backend).

## 📂 Структура проекта

```text
frontend/
 ┣ 📂 css/             # Стили
 ┃ ┣ 📜 main.css       # Глобальные стили (тема, шрифты, layout)
 ┃ ┣ 📜 auth.css       # Стили авторизации (модалка, формы)
 ┃ ┣ 📜 upload.css     # Стили Drag & Drop зоны
 ┃ ┗ 📜 gallery.css    # Стили списка изображений (таблица)
 ┃
 ┣ 📂 js/              # Логика
 ┃ ┣ 📜 api.js         # API Client (Singleton, Interceptor, Error Handling)
 ┃ ┣ 📜 auth.js        # Логика входа, регистрации и профиля
 ┃ ┣ 📜 index.js       # Главная страница (Feed, Viewer)
 ┃ ┣ 📜 upload.js      # Загрузка файлов (Drag&Drop)
 ┃ ┗ 📜 images.js      # Управление галереей (List View)
 ┃
 ┣ 📂 data/            # Ассеты
 ┃ ┗ 📂 img/           # Иконки и маскоты
 ┃
 ┣ 📜 index.html       # Главная страница (Feed)
 ┣ 📜 upload_form.html # Форма загрузки
 ┗ 📜 images.html      # Личный кабинет (Галерея)
```

## 🧩 Ключевые модули

### 1. API Client (`api.js`)
Центральный модуль для общения с бэкендом.
*   Автоматически добавляет `Authorization: Bearer` токен.
*   Обрабатывает ошибки 401 (открывает окно входа).
*   Адаптирует URL картинок для локальной разработки (`getImageUrl`).

### 2. Auth System (`auth.js`)
Управляет состоянием пользователя.
*   **Single Page Auth:** Вход и регистрация происходят в модальном окне без перезагрузки.
*   **Profile Menu:** Отображает аватар (букву) и выпадающее меню.
*   **Protection:** Проверяет URL на наличие `?login=true` для редиректов с защищенных страниц.

### 3. Viewer
Единый компонент просмотра изображений.
*   Реализован как скрытый блок `#image-viewer` на каждой странице.
*   Позволяет копировать прямую ссылку на файл.

## 📚 Разделы документации
*   **[🏗️ Архитектура модулей](./architecture/modules.md)** — Детальное описание скриптов.
*   **[🔌 API Client](./api_client.md)** — Методы и использование `api.js`.

---
[🏠 Вернуться на главную](../index.md)
