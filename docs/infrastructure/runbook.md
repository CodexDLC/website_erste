[🏠 Home](../index.md) > [Infrastructure](./index.md)

# 🏃 Runbook: Запуск и Остановка

Инструкция по эксплуатации проекта.

## 🐳 Запуск через Docker (Рекомендуемый)

Самый простой способ поднять всё окружение (БД, Бэкенд, Nginx).

### Запуск
```bash
# Сборка и запуск в фоновом режиме
docker-compose up -d --build
```
*   **Backend API:** http://localhost:8000 (или через Nginx на http://localhost)
*   **Swagger Docs:** http://localhost:8000/docs

### Просмотр логов
```bash
# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
```

### Остановка
```bash
# Остановить контейнеры
docker-compose stop

# Остановить и удалить контейнеры (данные в volumes сохранятся)
docker-compose down
```

### Полная очистка (Осторожно!)
```bash
# Удалить контейнеры, сети и VOLUMES (данные БД и картинки удалятся!)
docker-compose down -v
```

---

## 🐍 Локальный запуск (Для разработки)

Если нужно дебажить код или запускать без Докера.

### 1. База данных
Вам все равно понадобится Postgres. Проще всего запустить только её в докере:
```bash
docker-compose up -d db
```
Или используйте внешний Postgres (Neon, локальный). Убедитесь, что `.env` настроен корректно.

### 2. Backend
```bash
cd backend
# Активация venv
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate   # Windows

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера с Hot Reload
uvicorn main:app --reload
```

### 3. Frontend
Просто откройте `frontend/index.html` в браузере или используйте расширение "Live Server" в VS Code.
*   ⚠️ **Status:** Интеграция с API в процессе разработки (Phase 4). Пока работает на localStorage.

---
[⬅️ Назад к Infrastructure](./index.md)
