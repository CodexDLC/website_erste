[🏠 Home](../../index.md) > [Nginx](index.md)

# 🐳 Docker Integration

Интеграция Nginx в экосистему Docker Compose.

## Зависимости
В `docker-compose.yml` сервис Nginx должен зависеть от бэкенда, чтобы не стартовать раньше времени (хотя Nginx умеет ждать, лучше явно указать).

```yaml
depends_on:
  - backend
```

## Volumes (Тома)
Самая важная часть. Nginx должен иметь доступ к тем же файлам, что и другие сервисы.

1.  **Frontend Code:**
    *   `./frontend:/app/frontend`
    *   Позволяет править HTML/JS/CSS локально и сразу видеть изменения (Hot Reload "на минималках").

2.  **Uploads (Media):**
    *   `./uploads:/app/media`
    *   **Важно:** Это общая папка. Бэкенд *пишет* сюда файлы, а Nginx *читает* и отдает пользователю.

---
[🏠 Вернуться на главную](../../index.md)
