[🏠 Home](../index.md) > [Infrastructure](./index.md)

# ⚙️ Настройка CI/CD для своего форка

Если вы форкнули этот проект и хотите настроить автоматический деплой на свой VPS, вам нужно добавить **Secrets** в настройки репозитория GitHub.

## 🔑 GitHub Secrets

Перейдите в: `Settings` -> `Secrets and variables` -> `Actions` -> `New repository secret`.

Вам нужно добавить следующие 4 секрета:

| Имя секрета | Описание | Пример значения |
| :--- | :--- | :--- |
| **`HOST`** | IP-адрес вашего VPS сервера. | `123.45.67.89` |
| **`USERNAME`** | Имя пользователя для SSH подключения. | `root` |
| **`SSH_KEY`** | Ваш **приватный** SSH ключ (содержимое `id_rsa`). | `-----BEGIN OPENSSH PRIVATE KEY----- ...` |
| **`ENV_FILE`** | Полное содержимое файла `.env` для продакшена. | См. пример ниже 👇 |

### 📝 Пример содержимого `ENV_FILE`

Скопируйте это, замените пароли и домен на свои, и вставьте в секрет `ENV_FILE`.

```ini
# === Security ===
# Сгенерируйте новый: openssl rand -hex 32
SECRET_KEY=super_secret_production_key_change_me
DEBUG=False

# === Database ===
# Ссылка на вашу базу данных (внешнюю или локальную)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# === Logging ===
LOG_LEVEL_CONSOLE=WARNING
LOG_LEVEL_FILE=INFO
LOG_ROTATION="10 MB"

# === CORS & Domain ===
# ВАЖНО: Укажите ваш домен или IP
ALLOWED_ORIGINS=["https://your-domain.com", "http://your-ip"]
```

## 🖥️ Подготовка сервера (VPS)

Перед первым деплоем выполните на сервере:

1.  **Установите Docker и Docker Compose.**
2.  **Создайте папку проекта:**
    ```bash
    mkdir -p /opt/pinlite
    ```
3.  **Получите SSL сертификат (один раз):**
    ```bash
    # Остановите 80 порт, если занят
    docker stop pinlite-nginx

    # Запустите Certbot
    docker run -it --rm --name certbot \
      -v "/etc/letsencrypt:/etc/letsencrypt" \
      -v "/var/lib/letsencrypt:/var/lib/letsencrypt" \
      -p 80:80 \
      certbot/certbot certonly --standalone \
      -d your-domain.com \
      --email your-email@example.com --agree-tos --no-eff-email
    ```

После этого любой `git push` в ветку `release` автоматически обновит ваш сайт! 🚀

---
[⬅️ Назад к Infrastructure](./index.md)