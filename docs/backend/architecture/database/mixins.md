[🏠 Home](../../../index.md) > [Backend](../../index.md) > [Architecture](../index.md) > [Database](./index.md)

# 🧩 Database Mixins

Миксины позволяют переиспользовать общие поля в разных моделях, следуя принципу DRY (Don't Repeat Yourself).

## `TimestampMixin`
**File:** `backend/database/models/base.py`

Добавляет автоматическое отслеживание времени создания и обновления записи.

### Поля
*   **`created_at`** (`DateTime`): Дата создания. Заполняется автоматически БД (`server_default=func.now()`).
*   **`updated_at`** (`DateTime`): Дата последнего изменения. Обновляется автоматически БД при любом UPDATE (`onupdate=func.now()`).

### Пример использования

```python
from backend.database.models.base import Base, TimestampMixin

class User(Base, TimestampMixin):
    __tablename__ = "users"
    # ... остальные поля ...
    # created_at и updated_at добавятся автоматически
```

---
[🏠 Вернуться на главную](../../../index.md)
