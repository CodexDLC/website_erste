[🏠 Home](../../../index.md) > [Backend](../../index.md) > [Architecture](../index.md) > [Flows](./index.md)

# 🔄 Поток Данных (Data Flow)

Чтобы не было "лапши", данные всегда текут строго в одном направлении:

1.  **Request** прилетает в **API (Router)**.
2.  API валидирует входные данные через **Schemas (Pydantic)**.
3.  API передает чистые данные в **Service**.
4.  Service выполняет магию (хеширует, сохраняет файл, считает) и обращается к **БД**.
5.  Service возвращает результат (модель БД или доменный объект) обратно в **API**.
6.  Service возвращает результат (модель БД или доменный объект) обратно в **API**.
7.  API преобразует это в **Response Schema (JSON)** и отдает клиенту.

---
[🏠 Вернуться на главную](../../../index.md)
