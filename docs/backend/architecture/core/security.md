[🏠 Home](../../../../index.md) > [Backend](../../../index.md) > [Architecture](../../index.md) > [Core](./index.md)

# 🔐 Security Module

**File:** `backend/core/security.py`

Модуль содержит утилиты для обеспечения безопасности: хеширование паролей и генерацию токенов доступа.

## Функции

### `get_password_hash(password: str) -> str`
Хеширует пароль с использованием алгоритма **Bcrypt**.
*   Используется при регистрации пользователя.

### `verify_password(plain_password, hashed_password) -> bool`
Проверяет соответствие введенного пароля хешу из базы данных.
*   Используется при входе (Login).

### `create_access_token(subject, expires_delta) -> str`
Генерирует **JWT (JSON Web Token)**.
*   `subject`: Обычно ID пользователя.
*   `expires_delta`: Время жизни токена (по умолчанию 30 минут).
*   Алгоритм подписи: `HS256`.

---
[🏠 Вернуться на главную](../../../../index.md)
