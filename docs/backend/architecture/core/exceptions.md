[🏠 Home](../../../../index.md) > [Backend](../../../index.md) > [Architecture](../../index.md) > [Core](./index.md)

# ⚠️ Exceptions Module

**File:** `backend/core/exceptions.py`

Модуль определяет базовые классы исключений и глобальный обработчик ошибок.

## 📦 Формат ответа (JSON)
Все ошибки API возвращаются в едином формате. Фронтенд всегда должен ожидать поле `error`.

```json
{
  "error": {
    "code": "validation_error",
    "message": "Invalid email format",
    "fields": ["email"]  // Опционально, зависит от типа ошибки
  }
}
```

## 🚦 Frontend Handling Guide (Инструкция)

Как фронтенд должен реагировать на разные коды ошибок:

| HTTP Code | Error Code (`code`) | Описание | Реакция Фронтенда (UX) |
| :--- | :--- | :--- | :--- |
| **401** | `auth_error` | Токен невалиден или протух. | 1. Попробовать обновить токен (Refresh).<br>2. Если не вышло — **Редирект на /login**. |
| **403** | `permission_denied` | Не хватает прав (например, удаление чужой картинки). | Показать всплывающее уведомление (Toast): *"У вас нет прав для этого действия"*. |
| **404** | `not_found` | Ресурс не найден. | **Страница:** Показать компонент 404.<br>**Список:** Показать "Ничего не найдено". |
| **409** | `business_conflict` | Конфликт (например, email занят). | Показать ошибку под конкретным полем или общий Alert. |
| **422** | `validation_error` | Ошибка валидации данных. | **Подсветить поля красным.**<br>В поле `extra.fields` придет список ошибочных полей. |
| **500** | `server_error` | Внутренняя ошибка сервера. | Показать общий экран "Что-то пошло не так, мы уже чиним". |

## 🐍 Классы ошибок (Backend)

*   **`BaseAPIException`**: Родительский класс.
*   **`NotFoundException` (404)**: `error_code="not_found"`
*   **`ValidationException` (422)**: `error_code="validation_error"`
*   **`BusinessLogicException` (409)**: `error_code="business_conflict"`
*   **`PermissionDeniedException` (403)**: `error_code="permission_denied"`
*   **`AuthException` (401)**: `error_code="auth_error"`

## Использование в коде

```python
from app.core.exceptions import NotFoundException, BusinessLogicException

# Пример 1: Не найдено
if not user:
    raise NotFoundException(detail="User not found")

# Пример 2: Конфликт
if user_exists:
    raise BusinessLogicException(detail="Email already registered")
```

---
[🏠 Вернуться на главную](../../../../index.md)
