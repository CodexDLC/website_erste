[🏠 Home](../../../../index.md) > [Backend](../../../index.md) > [Architecture](../../index.md) > [Core](./index.md)

# 📜 Config Module

**File:** `backend/core/config.py`

Модуль отвечает за загрузку и валидацию конфигурации приложения. Использует библиотеку `pydantic-settings`.

## Основные возможности
*   Читает переменные из файла `.env`.
*   Читает переменные окружения (Environment Variables).
*   Валидирует типы данных (например, проверяет, что `MAX_UPLOAD_SIZE` — это число).
*   Автоматически вычисляет пути к файлам (например, путь к логам).

## Использование

```python
from app.core.config import settings

# Получить секретный ключ
key = settings.SECRET_KEY

# Получить путь к папке загрузок
upload_path = settings.UPLOAD_DIR
```

## Ключевые настройки
*   `SECRET_KEY`: Ключ для подписи JWT.
*   `DATABASE_URL`: Строка подключения к БД.
*   `DEBUG`: Режим отладки.
*   `UPLOAD_DIR`: Путь к папке с картинками.

---
[🏠 Вернуться на главную](../../../../index.md)
