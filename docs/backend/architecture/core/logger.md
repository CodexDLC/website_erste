[🏠 Home](../../../../index.md) > [Backend](../../../index.md) > [Architecture](../../index.md) > [Core](./index.md)

# 🪵 Logger Module

**File:** `backend/core/logger.py`

Настройка структурированного логирования с использованием библиотеки **Loguru**.

## Особенности
*   **Перехват стандартного logging:** Все логи от библиотек (FastAPI, SQLAlchemy) перенаправляются в Loguru.
*   **Разделение потоков:**
    *   `Console`: Цветной вывод для разработки.
    *   `File (debug.log)`: Полный лог всех событий.
    *   `File (errors.json)`: Лог ошибок в формате JSON для удобного парсинга.
*   **Ротация:** Файлы логов автоматически ротируются при достижении размера (настраивается в config).

## Использование

```python
from loguru import logger

logger.info("User logged in", user_id=123)
logger.error("Database connection failed", error=str(e))
```

---
[🏠 Вернуться на главную](../../../../index.md)
