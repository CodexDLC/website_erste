[🏠 Home](../../../../index.md) > [Backend](../../../index.md) > [Architecture](../../index.md) > [Domains](../index.md)

# 👤 Домен: Users (Identity & Access)

Отвечает за то, "КТО" совершает действие.

## Структура
*   **Contracts (`contracts/`)**: Интерфейсы репозиториев (Protocol).
*   **Services (`services/`)**: Чистая бизнес-логика.
*   **Schemas (`schemas/`)**: Pydantic модели для валидации данных.
*   **API (`api/`)**: FastAPI роутеры.

## Основные сущности (DB Models)
*Модели находятся в `backend/database/models/user.py`*
*   **User** (`id`, `email`, `hashed_password`, `is_active`, `created_at`).

## Бизнес-логика (Services)
*   **AuthService:**
    *   Регистрация (валидация email, хеширование пароля).
    *   Аутентификация (проверка пароля, выдача Access/Refresh токенов).
*   **UserService:**
    *   Получение профиля текущего пользователя (`me`).
    *   (В будущем) Смена пароля, обновление аватара.

## API Ручки (Routes)
*   `POST /auth/register`
*   `POST /auth/login` (возвращает JWT)
*   `GET /users/me` (требует Auth Header)

---
[🏠 Вернуться на главную](../../../../index.md)
