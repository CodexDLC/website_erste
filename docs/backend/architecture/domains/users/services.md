[🏠 Home](../../../../index.md) > [Backend](../../../index.md) > [Architecture](../../index.md) > [Domains](../index.md) > [Users](./index.md)

# 🧠 Business Logic (Services)

Сервис содержит чистую бизнес-логику. Один публичный метод часто соответствует одной ручке API.

## `AuthService`

### `register_user(schema: UserCreate) -> UserResponse`
1.  Проверить в репозитории, нет ли уже такого email (`get_by_email`).
    *   Если есть — ошибка `409 Conflict`.
2.  Захешировать пароль через утилиту безопасности (`security.get_password_hash`).
3.  Вызвать репозиторий `create` с хешем вместо пароля.
4.  Вернуть созданного пользователя.

### `authenticate_user(schema: UserLogin) -> TokenSchema`
1.  Найти пользователя по email (`get_by_email`).
    *   Если нет — ошибка `401 Unauthorized`.
2.  Проверить пароль (`security.verify_password`).
    *   Если не совпал — ошибка `401 Unauthorized`.
3.  Генерировать **Access Token** (JWT stateless).
4.  Генерировать **Refresh Token** (random string).
5.  Сохранить Refresh Token в БД через `ITokenRepository.create`.
6.  Вернуть пару токенов.

### `refresh_token(token: str) -> TokenSchema`
1.  Найти токен в БД (`ITokenRepository.get_by_token`).
    *   Если нет — ошибка `401 Unauthorized`.
2.  Проверить срок действия (`expires_at`).
    *   Если протух — удалить токен и вернуть ошибку `401`.
3.  **Ротация:** Удалить старый токен (`delete`).
4.  Сгенерировать новую пару (Access + Refresh).
5.  Сохранить новый Refresh в БД.
6.  Вернуть новую пару.

---
[🏠 Вернуться на главную](../../../../index.md)
