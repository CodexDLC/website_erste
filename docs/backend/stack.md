[🏠 Home](../index.md) > [Backend](./index.md)

# 🛠️ Project Stack & Deployment

Этот документ фиксирует выбранные технологии и стратегию запуска для MVP.

## 1. Технологический Стек (Technology Stack)
Мы выбираем классическую, надежную архитектуру для высокой производительности и простоты поддержки.

### Backend (Server Side)
*   **Language:** Python 3.11+ (строгая типизация).
*   **Framework:** FastAPI (Асинхронный, быстрый, автоматическая документация Swagger).
*   **Server:** Uvicorn (ASGI сервер).
*   **Database Interface:** SQLAlchemy 2.0 (Async) — современный ORM.
*   **Validation:** Pydantic v2 — валидация данных и настроек.
*   **Migrations:** Alembic — управление изменениями в структуре БД.

### Database (Storage)
*   **RDBMS:** PostgreSQL 15.
*   **Driver:** asyncpg — самый быстрый асинхронный драйвер для Python.

### Frontend (Client Side)
*   **Core:** Vanilla JS (ES6+), HTML5, CSS3.
*   **Build System:** Отсутствует (No Build Step). Файлы отдаются "как есть". Это упрощает отладку и деплой на старте.
*   **Interaction:** `fetch API` для общения с бэкендом.

## 2. Стратегия запуска (Deployment Strategy)
Мы используем контейнеризацию с первого дня.

### Этап 1: Локальная разработка (Local Dev)
*   **БД:** В Docker контейнере (`docker-compose up db`).
*   **Backend:** Локально через `python main.py` (для работы дебаггера и Hot Reload).
*   **Frontend:** Локально (LiveServer).

### Этап 2: MVP на сервере (Docker Compose)
*   Весь стек (Nginx + Backend + DB) запускается одной командой: `docker-compose up -d`.
*   **Nginx** выступает как Gateway, проксируя запросы и раздавая статику.
*   Данные (картинки, база) хранятся в персистентных **Docker Volumes**.

### Этап 3: CI/CD & Orchestration (Future)
*   Автоматический деплой через GitHub Actions.
*   Возможный переход на Kubernetes (K8s) при высокой нагрузке.

---
[🏠 Вернуться на главную](../index.md)
