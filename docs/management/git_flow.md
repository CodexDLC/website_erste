[🏠 Home](../index.md) > [Management](./index.md)

# 🌿 Git Flow & Branching Strategy

Мы используем стратегию **3 основных веток** для обеспечения стабильности и качества кода.

## 🌳 Структура веток

### 1. `develop` 🛠️ (Разработка)
*   **Назначение:** Основная ветка для интеграции новых фич. Здесь живет "свежий", но потенциально нестабильный код.
*   **Правила:**
    *   Все новые фичи (`feature/*`) вливаются сюда через Pull Request (PR).
    *   **CI Checks:** Запускаются линтеры (`Ruff`, `Mypy`). Тесты базы данных **не** запускаются для скорости.
*   **Защита:** Требует прохождения линтеров.

### 2. `main` 🧪 (Staging / Pre-Release)
*   **Назначение:** Стабильная ветка, готовая к релизу. Аналог Staging-окружения.
*   **Правила:**
    *   Сюда попадает код только из `develop` через PR.
    *   **CI Checks:** Запускается **полный набор тестов** (`Pytest` с поднятием БД) и проверка сборки Docker-образа.
*   **Защита:** Строгая. Мерж только при прохождении всех тестов. Прямой пуш запрещен.

### 3. `release` 🚀 (Production)
*   **Назначение:** Код, который работает на боевом сервере.
*   **Правила:**
    *   Сюда попадает код **только из `main`** через PR.
    *   **CD Action:** При пуше в эту ветку запускается автоматический деплой на VPS.
*   **Защита:** Максимальная. Мерж разрешен **только** из ветки `main` (контролируется GitHub Actions).

---

## 🔄 Рабочий процесс (Workflow)

1.  **Новая задача:**
    *   Создай ветку от `develop`: `git checkout -b feature/my-cool-feature develop`.
    *   Пиши код, коммить.
    *   Перед пушем запусти локальную проверку: `.\check_local.ps1` (для Windows; на других ОС используйте `pwsh` или адаптируйте команды).

2.  **Интеграция (Develop):**
    *   Сделай Push и открой PR в `develop`.
    *   GitHub Actions проверит стиль и типы.
    *   Смержи PR.

3.  **Стабилизация (Main):**
    *   Когда фичи готовы, открой PR `develop` -> `main`.
    *   GitHub Actions прогонит тяжелые тесты.
    *   Если всё ок — мержи.

4.  **Релиз (Release):**
    *   Открой PR `main` -> `release`.
    *   GitHub Actions проверит, что источник именно `main`.
    *   После мержа начнется магия: сборка Docker-образа -> Push в GHCR -> Обновление VPS.

---
[⬅️ Назад к Management](./index.md)