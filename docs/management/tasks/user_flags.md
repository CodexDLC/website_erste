[🏠 Home](../../index.md) > [Management](../index.md) > [Tasks](./index.md)

# 🚩 Task: Refactor User Creation Flags

**Status:** 📋 Backlog
**Priority:** Low
**Related Tech Debt:** Hardcoded User Flags

## 📝 Описание проблемы
В методе `UserRepository.create` жестко зашиты значения флагов:
```python
is_active=True
is_superuser=False
```
Это делает невозможным создание неактивных пользователей (для подтверждения email) или администраторов через этот метод.

## 🎯 Цель
Сделать метод создания пользователя гибким.

## 📋 План реализации

1.  **Schema:** Добавить опциональные поля `is_active` и `is_superuser` в `UserCreate` (или создать отдельную DTO для репозитория).
2.  **Repository:** Принимать эти флаги в методе `create` и использовать их при создании модели `User`.
3.  **Service:** В `AuthService.register_user` явно передавать нужные дефолтные значения (Active=True, Superuser=False), чтобы сохранить текущее поведение для публичной регистрации.

---
[⬅️ Вернуться к списку задач](./index.md)
