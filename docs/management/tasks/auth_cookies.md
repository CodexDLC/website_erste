[🏠 Home](../../index.md) > [Management](../index.md) > [Tasks](./index.md)

# 🍪 Task: Secure Auth Storage (HttpOnly Cookies)

**Status:** 📋 Backlog
**Priority:** High
**Related Tech Debt:** Auth Storage (localStorage)

## 📝 Описание проблемы
В MVP версии токены (Access и Refresh) хранятся на фронтенде в `localStorage`.
Это делает их уязвимыми для XSS (Cross-Site Scripting) атак. Если злоумышленник сможет выполнить JS-код на странице, он сможет украсть токены.

## 🎯 Цель
Перенести хранение токенов в безопасные `HttpOnly` Cookies.

## 📋 План реализации

1.  **Backend:**
    *   Изменить эндпоинты `/login` и `/refresh`. Вместо возврата токенов в теле JSON, устанавливать их через `response.set_cookie(...)`.
    *   Параметры Cookie: `httponly=True`, `secure=True` (для HTTPS), `samesite='Lax'` или `'Strict'`.
    *   Изменить эндпоинт `/logout`: удалять куки (`response.delete_cookie`).
2.  **Frontend:**
    *   Перестать читать токены из ответа JSON.
    *   Браузер будет автоматически отправлять куки с каждым запросом.

---
[⬅️ Вернуться к списку задач](./index.md)
