[🏠 Home](../../index.md) > [Management](../index.md) > [Tasks](./index.md)

# 💡 Feature: Headless / Microservice Mode

**Status:** 💡 Idea / Backlog
**Priority:** Low (Architectural)
**Phase:** Future (v2.0)

## 📝 Описание
Реализация режима работы сервера "только API" (Headless) для использования в качестве микросервиса хранения изображений для сторонних приложений (Mobile Apps, Other Sites).

## 🎯 Цель
Позволить использовать PinLite как замену S3/Cloudinary для своих проектов.

## 📋 Концепция

### 1. Режимы работы (Config)
В `.env` добавляется переменная `APP_MODE`:
*   `FULL` (Default): Текущий режим. Фронтенд, Пользователи, Регистрация.
*   `HEADLESS`: Только API для загрузки и отдачи.

### 2. Изменения в Headless режиме
*   **Frontend:** Отключен (не раздается статика).
*   **Auth:**
    *   Отключена регистрация пользователей.
    *   Вместо JWT (User Token) используется **API Key** (Service Token), прописанный в `.env`.
    *   Пример заголовка: `X-API-Key: my-secret-service-key`.
*   **Storage:**
    *   Все файлы привязываются к единому "System User" или хранятся без привязки к пользователю.

### 3. Use Cases
1.  Бэкенд для мобильного приложения.
2.  CDN для интернет-магазина.
3.  Личное хранилище скриншотов (ShareX custom uploader).

---
[⬅️ Вернуться к списку задач](./index.md)
