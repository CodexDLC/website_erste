[🏠 Home](../../index.md) > [Management](../index.md) > [Tasks](./index.md)

# 💡 Feature: Social Mechanics (Likes & Comments)

**Status:** 💡 Idea / Backlog
**Priority:** Medium
**Phase:** Future (v2.0)

## 📝 Описание
Превращение сервиса из простого хостинга в социальную платформу. Пользователи должны иметь возможность оценивать контент.

## 🎯 Цели
1.  Повышение вовлеченности (Engagement).
2.  Сортировка ленты по популярности (Trending).

## 📋 План реализации

### 1. Database
*   Таблица `likes`:
    *   `user_id` (FK -> users)
    *   `image_id` (FK -> images)
    *   `created_at`
    *   Unique Constraint: `(user_id, image_id)` (один лайк на картинку).

### 2. Backend API
*   `POST /media/{id}/like` — Поставить лайк.
*   `DELETE /media/{id}/like` — Убрать лайк.
*   Обновить `ImageRead` схему:
    *   `likes_count: int` (агрегация).
    *   `is_liked: bool` (персонализированное поле, требует `current_user`).

### 3. Frontend
*   Кнопка "Heart" ❤️ на карточке в сетке и во вьювере.
*   Счетчик лайков.
*   Анимация нажатия.
*   Сортировка ленты: "Newest" vs "Popular".

## ❓ Вопросы
*   Нужны ли дизлайки?
*   Нужны ли комментарии?

---
[⬅️ Вернуться к списку задач](./index.md)
