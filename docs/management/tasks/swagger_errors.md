[🏠 Home](../../index.md) > [Management](../index.md) > [Tasks](./index.md)

# 📝 Task: Swagger Error Schemas

**Status:** ✅ Done
**Priority:** Medium
**Related Tech Debt:** Poor API Documentation

## 📝 Описание проблемы
По умолчанию Swagger (OpenAPI) показывает для ошибок (400, 404 и т.д.) стандартную схему `HTTPValidationError` или просто текстовое описание.
Наш бэкенд возвращает ошибки в формате:
```json
{
  "error": {
    "code": "error_code",
    "message": "Human readable message",
    "fields": [...]
  }
}
```
Фронтенд-разработчики не видят эту структуру в документации.

## 🎯 Цель
Явно описать схему ошибок в Swagger UI для всех эндпоинтов.

## 📋 План реализации
1.  Создать Pydantic-модель `ErrorResponse` в `backend/core/schemas/error.py`.
2.  Подключить эту модель в `backend/main.py` через параметр `responses` в `FastAPI(...)`.
3.  Проверить отображение в `/docs`.

---
[⬅️ Вернуться к списку задач](./index.md)
