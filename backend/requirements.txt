# Core
fastapi
uvicorn[standard]
pydantic
pydantic-settings
email-validator

# Database
sqlalchemy
asyncpg
alembic

# Security
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1

# Logging
loguru

# Utilities
python-multipart
pillow
aiofiles

# Magic (File type detection)
# python-magic-bin нужен ТОЛЬКО для Windows.
# python-magic нужен для Linux/Mac.
python-magic-bin; sys_platform == 'win32'
python-magic; sys_platform != 'win32'